server:
  port: 8080
langchain4j:
  open-ai:
    chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: ${API-KEY}
      model-name: tongyi-xiaomi-analysis-pro
      log-requests: true
      log-responses: true
#      流式输出
    streaming-chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: ${API-KEY}
      model-name: tongyi-xiaomi-analysis-pro
      log-requests: true
      log-responses: true
#      向量模型
#    embedding-model:
#      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
#      api-key: ${API-KEY}
#      model-name: tongyi-embedding-vision-flash-2026-03-06
#      log-requests: true
#      log-responses: true
logging:
  level:
    dev.langchain4j: debug

spring:
  data:
    redis:
      host: localhost
      port: 6379
